spring:
  application:
    name: swm-backend

  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: validate

  flyway:
    baseline-on-migrate: true
    out-of-order: true
springdoc:
  use-fqn: true
